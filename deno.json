{
  "tasks": {
    "update": "bash -c 'shopt -s globstar; deno run -A https://deno.land/x/udd@0.7.5/main.ts **/*.ts'; deno task cache",
    "cache": "bash -c 'shopt -s globstar; deno cache **/*.ts'",
    "deps_lint": "deno run -A scripts/deps_lint.ts --deps=./deps --src=./src",
  },
  "fig": {
    "update": {
      "description": "Update dependency versions",
      "icon": "⬆️"
    },
    "cache": {
      "description": "Cache dependencies",
      "icon": "⬇️"
    },
    "deps_lint": {
      "description": "Lint `deps` folder",
      "icon": "🧼",
    }
  }
}
